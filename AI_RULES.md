# قواعد تغییر رقابت‌نامه برای هوش مصنوعی

این فایل الزام‌آور است. پیش از هر ویرایش، `PROJECT_GUIDE.md` را نیز کامل بخوان.

## محتوای حقوقی

- متن قانون، رأی، مصوبه و شرح نویسنده را خلاصه، بازنویسی، نرم‌سازی یا تکمیل نکن مگر نادر جعفری صریحاً بخواهد
- متن مصوبات CRA باید از خروجی واردکننده ساخته شود و اصلاح دستی محتوای فایل‌های `content/cra/documents` مجاز نیست
- متن OCR بازبینی‌شده فقط از مسیر `scripts/apply-cra-ocr.py` در `content/cra/ocr-overrides` قرار می‌گیرد. این لایه برای رکوردهای ثبت‌شده در `manifest.json` بر خروجی واردکننده مقدم است و فایل اصلی را تغییر نمی‌دهد
- یادداشت‌های کاری، دستورهای خطاب به هوش مصنوعی و نشانه‌های ویرایش نباید وارد متن عمومی مصوبه شوند
- رابطه‌های CRA فقط بازتاب برچسب‌های خود سامانه منبع‌اند. از «مرتبط»، «تاثیرگذار» یا «تاثیرپذیر» بودن، اصلاح، نسخ، تایید یا اعتبار جاری استنباط نکن
- نام رسمی پیوست دارای عبارت «تنقیحی» یا «تنقیح‌شده» می‌تواند مبنای ثبت وجود متن تنقیحی و رابطه میان مصوبه پایه و مصوبات نام‌برده در همان نام باشد. این رابطه را در `versions` قرار نده
- ارجاع‌های استخراج‌شده از متن فقط وجود شماره مصوبه و جلسه را ثابت می‌کنند. آنها را به اصلاح، نسخ، تایید، استناد حقوقی یا اثرگذاری تبدیل نکن
- زنجیره اسناد تاثیرگذار فقط مسیر مطالعه بر پایه روابط منبع است. آن را متن تنقیح‌شده یا اعلام آخرین مقرره لازم‌الاجرا معرفی نکن
- متن تنقیح‌شده و خلاصه حقوقی فقط پس از کنترل محتوایی مستقل منتشر می‌شوند. کنار هم گذاشتن خودکار متن پایه و اصلاحیه‌ها تنقیح معتبر نیست
- متن تنقیحی غیررسمی باید از متن‌های رسمی جدا، با برچسب روشن و با هشدار رجوع به مصوبه اصلی و اصلاحیه‌ها نمایش داده شود
- خطاهای احتمالی متن منبع را پنهانی اصلاح نکن
- از یک استناد یا اشاره، قاعده عام یا نسبت حقوقی قوی استنباط نکن
- `commentary-reference` فقط نشان می‌دهد شرح به سند اشاره کرده است
- `applies`، `interprets`، `affirms`، `reverses` و رابطه‌های مشابه فقط با شاهد روشن ثبت شوند
- تاریخ‌های نمایشی وب باید به ترتیب سال، ماه و روز و با رقم فارسی باشند. قاعده روز، ماه و سال مخصوص اسناد Word است و در سایت اعمال نمی‌شود
- اعداد رابط کاربری باید فارسی باشند

## معماری

- منبع اصلی شبکه `lib/knowledge/registry.ts` است
- رکوردهای انبوه CRA در `content/cra/index.json` نگهداری و از طریق `lib/cra/data.ts` به دفتر مرکزی شبکه متصل می‌شوند. آنها را به‌صورت دستی در `registry.ts` تکرار نکن
- نوع‌ها در `lib/knowledge/types.ts` تعریف می‌شوند و نباید با ساختارهای موازی تکثیر شوند
- فهرست‌های نهاد، موضوع، بازار و اسناد باید از `lib/knowledge/queries.ts` ساخته شوند
- کارت یا لینک یک سند را در چند صفحه به‌صورت دستی تکرار نکن
- سند را زیر یک ماده یا نهاد محبوس نکن. سند مستقل است و با شناسه‌ها رابطه می‌گیرد
- رکورد بدون محتوای واقعی باید `draft` بماند
- مقصد رابطه‌ای که سند محلی ندارد نباید در رابط عمومی لینک یا برچسب وضعیت بسازد. شناسه تکراری فقط از طریق `content/cra/relationship-curation.json` به سند موجود متصل می‌شود و مقصد مبهم یا آزمایشی از نمایش حذف می‌شود
- برای رکورد پیش‌نویس صفحه عمومی خالی، مسیر قابل کلیک یا ورودی نقشه سایت نساز
- افزودن نهاد یا نوع محتوای تازه نباید نشانی‌های مرجع فعلی را تغییر دهد
- هفت دسته CRA بازتاب مستقیم پوشه‌های منبع‌اند و نباید بدون تصمیم صریح مالک سایت ادغام، بازنام‌گذاری یا با طبقه‌بندی تفسیری جایگزین شوند
- شمار مصوبات و شمار هر دسته باید از `content/cra/index.json` محاسبه شود و به‌صورت عدد ثابت در رابط کاربری نوشته نشود

## نشانی و سئو

- نشانی مرجع ماده باید زیر منبع قانونی و نشانی مرجع رأی باید با ترتیب نهاد، سال و شماره ساخته شود
- نشانی مرجع مصوبه CRA باید با الگوی `/resolutions/cra/{year}/{slug}` ساخته شود. دسته فقط در مسیر فهرست‌هاست و وارد نشانی سند نمی‌شود
- نشانی‌های قدیمی ثبت‌شده در `legacyRoutes` را حذف یا دوباره به صفحه مستقل تبدیل نکن
- canonical، نقشه سایت، جست‌وجو و لینک‌های داخلی فقط باید نشانی مرجع تازه را منتشر کنند
- انتقال هر نشانی قدیمی باید مستقیم و بدون زنجیره به مقصد نهایی باشد
- لینک داخلی با اسلش پایانی نساز
- `trailingSlash` را بدون آزمون دامنه GitHub Pages تغییر نده
- `public/CNAME` را حذف نکن
- canonical و نقشه سایت فقط به `https://naderjafari.com` اشاره کنند
- فقط محتوای واقعاً منتشرشده در `app/sitemap.ts` قرار گیرد
- صفحه تکراری با محتوای یکسان نساز

## ظاهر و دسترس‌پذیری

- زبان بصری سرمه‌ای تیره و طلایی محدود را حفظ کن مگر مالک سایت تغییر بخواهد
- Estedad برای تیتر و Vazirmatn برای متن است
- اندازه متن‌های رابط را کمتر از متغیر `--font-caption` نکن
- متن‌های اصلی را با واحد نسبی نگه دار
- هر تغییر باید در موبایل و دسکتاپ بررسی شود
- فقط یک هدر، یک ناحیه اصلی و یک فوتر در هر صفحه وجود داشته باشد
- HTML تودرتوی نامعتبر مانند `main` داخل `main` نساز
- صفحه جست‌وجو باید فقط محتوای منتشرشده را ببیند و رکوردهای پیش‌نویس را وارد نمایه نکند
- آرشیو CRA جامع و منبع‌محور است، ولی بخش آرای شورای رقابت همچنان گزینشی است. این دو سیاست را با هم خلط نکن

## روند کار

- پیش از تغییر، `git status` و فایل‌های مرتبط را بررسی کن
- تغییرهای کاربر را پاک یا بازنشانی نکن
- برای ویرایش فایل از patch استفاده کن
- پس از هر تغییر محتوایی یا معماری `npm run build` را اجرا کن
- خطای بیلد را با حذف محتوا یا خاموش‌کردن بررسی TypeScript پنهان نکن
- خروجی مسیرهای کلیدی و `sitemap.xml` را بررسی کن
- رکورد پیش‌نویس نباید ناخواسته در پوشه `out` ساخته شود
- فقط پس از بیلد موفق و کنترل مسیرها تغییر را برای انتشار آماده کن

## هنگام ابهام

اگر انتخاب بر معنای حقوقی، دامنه پوشش، طبقه‌بندی سند یا نشانی عمومی اثر جدی دارد، حدس نزن. گزینه‌ها و اثر هرکدام را کوتاه برای نادر جعفری توضیح بده و تصمیم او را بگیر.
